/* eslint-disable import/no-anonymous-default-export */
export default (():any => ({
  autoCreate: true,
  fields: [
    {
      label: '密码',
      required: true,
      name: 'password',
    },
  ],
}));
